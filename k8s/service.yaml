apiVersion: v1
kind: Service
metadata:
  name: reviewservice-backend
spec:
  selector:
    app: reviewservice-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
